@page "/listmhs"
@inject HttpClient Http

<PageTitle>List Mahasiswa</PageTitle>   

<h1>List Mahasiswa</h1>

<p>This component shows the list of collage students.</p>

@if (mhs == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <div class="input-group col">
        <input type="text" class="form-control" placeholder="Search Mahasiswa" @bind="SearchString" />
        <button @onclick ="CariData">SEARCH</button>
    </div>
    <table class="table">
        <thead>
            <tr>
                <th>ID</th>
                <th>Nama</th>
                <th>NIM</th>
                <th>Alamat</th>
                <th>Email</th>
                <th>Foto</th>
            </tr>
        </thead>
        <tbody> 
            @foreach (var mh in mhs)
            {
                <tr>
                    <td>@mh.id</td>
                    <td>@mh.nama</td>
                    <td>@mh.nim</td>
                    <td>@mh.alamat</td>
                    <td>@mh.email</td>
                    <td>@mh.foto</td>
                </tr>
            }
        </tbody>
        <div class="card-footer text-center">
                <a href="addMhs" class="btn btn-primary m-1">ADD</a>
        </div>
    </table>
}

@code {
 

    protected override async Task OnInitializedAsync()
    {
        mhs = await Http.GetFromJsonAsync<List<Mahasiswa>>("https://kpsi.fti.ukdw.ac.id/api/progmob/mhs/72200364");
            searchUserData = mhs;
    }

    protected List<Mahasiswa> searchUserData = new();
    protected List<Mahasiswa> mhs = new();
    protected string SearchString { get; set;} = string.Empty;
    protected void CariData(){
        if (!string.IsNullOrEmpty(SearchString))
        {
            mhs = searchUserData.Where(x => x.nim.IndexOf(SearchString, StringComparison.OrdinalIgnoreCase) != -1).ToList();
        }
        else
        {
            mhs = searchUserData;
        }
    }
} 


